{% extends 'base.html.twig' %}

{% block title %}Niveaux{% endblock %}

{% block body %}
    <h1>Niveaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ niveau.code }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ niveau.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('niveaux_index') }}">back to list</a>

    <a href="{{ path('niveaux_edit', {'id': niveau.id}) }}">edit</a>

    {{ include('niveaux/_delete_form.html.twig') }}
{% endblock %}
