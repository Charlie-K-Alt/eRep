{% extends 'base.html.twig' %}

{% block title %}Niveaux -> Détail{% endblock %}

{% block contenuNiveaux %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ niveau.code }}</td>
            </tr>
            <tr>
                <th>Libellé</th>
                <td>{{ niveau.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary bg-gradient-info" href="{{ path('niveaux_index') }}"><- Retour </a>


    {{ include('niveaux/_delete_form.html.twig') }}
{% endblock %}
