{% extends 'base.html.twig' %}

{% block title %}Apprenants index{% endblock %}

{% block contenuApprenant %}


<div class="row long75pc">
    <div class="col-md-12 search-table-col">
        <div class="row">
            <div class="col-2" ><a class="btn btn-primary" id="btnAddMat" href="{{ path('apprenants_new') }}">Ajouter</a></div>
            <div class="col-2"></div> <div class="col-2"></div>
            <div class="col-4 offset-2"><input type="text" class="search form-control" placeholder="Search by typing here.."></div>
        </div><span class="counter pull-right"></span>
        <div class="table-responsive table-bordered table table-hover table-bordered results ">
                <table class="table table-bordered table-hover ">
                <thead class="bill-header cs">

                    <tr>
                        <th>Nom</th>
                        <th>Prenoms</th>
                        <th>Telephone</th>
                        <th>Email</th>
                        <th>Password</th>
                        <th>Pseudo</th>
                        <th>Datenaissance</th>
                        <th>Sexe</th>
                        <th>actions</th>
                    </tr>
                </thead>
                <tbody>
                        {% for apprenant in apprenants %}
                            <tr>
                                <td>{{ apprenant.nom }}</td>
                                <td>{{ apprenant.prenoms }}</td>
                                <td>{{ apprenant.telephone }}</td>
                                <td>{{ apprenant.email }}</td>
                                <td>{{ apprenant.password }}</td>
                                <td>{{ apprenant.pseudo }}</td>
                                <td>{{ apprenant.datenaissance ? apprenant.datenaissance|date('Y-m-d') : '' }}</td>
                                <td>{{ apprenant.sexe }}</td>
                                <td style="width: 21%;">
                                    <span class="long7pc"><a href="{{ path('apprenants_show', {'id': apprenant.id}) }}"><i class="fas fa-file-alt fa-lg"></i></a></span>
                                    <span  class="long7pc"><a href="{{ path('apprenants_edit', {'id': apprenant.id}) }}"><i class="fas fa-edit fa-lg"></i></a></span>
                                    <span  class="long7pc"><a href=""><i class="fas fa-trash fa-lg"></i></a></span>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="10">Pas de résultat</td>
                            </tr>
                        {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

{% endblock %}


 {#fvjwv#}

